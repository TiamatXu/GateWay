# 待确认队列

Claude 自主推进 M0 期间攒下的、需要你拍板或知情的事项。做完一项就划掉。

格式：`- [ ] (日期) 【类别】事项 —— 我的处理 / 需要你做什么`

---

## 需要你决定

- [ ] (08-23)【分支】M0 代码提交在 `m0/skeleton-contracts` 分支，未直接进 main。
      单人无远程仓库，分支意义不大——要合回 main 直接说，我 fast-forward 过去。

- [ ] (08-26)【M1 砍掉 Redis】你点名砍分片，并给了原则「基建扛得住就不过多考虑」。
      我按同一原则把**租约、双模式切换、Redis Coordinator** 一并移出了 M1——它们
      三个的目的都是给 PG 减压，都建立在「PG 扛不住」这个还没测过的前提上。
      Redis 这一项你上一轮是明确选了要做的，是我基于新的调研结论推翻的，
      所以单独列出来。不同意就说，改回去只是把 §3.4 那节删掉。
      理由见 `docs/superpowers/specs/2026-08-26-m1-ledger-scope-design.md` §3。

## M1 开工前需要注意（不必现在回复）

- [x] ~~(08-23)【预扣过粗】`estimate_max` 目前按固定上限估算（输入 128k token、
      输出取请求声明值）。后果：$3/M 的模型每个请求要冻结约 $0.384，余额只有
      $0.30 的用户一个请求都发不出去，哪怕实际只花 $0.001。
      M0 spec 明确把这项列为 SIMPLIFIED，所以我没有扩大范围。
      **修法很便宜**：JSON 端点的请求体本来就已经读进内存了，直接用 tiktoken
      数一遍真实输入 token 即可。建议列为 M1 第一项。

- [x] ~~(08-23)【幂等键没接上】冻结的 idempotency_key 现在填的是每次新生成的
      request_id，等于永远不会命中去重。真正的幂等需要客户端传
      `Idempotency-Key` 头。机制已经建好并有测试覆盖，只是没接入口。

## 需要你提供材料

- [ ] (08-23)【真实样本】M0 上游按 OpenAI 兼容协议的官方文档实现。实现完成后需要你提供
      真实的请求/响应样本核对，尤其是 **SSE 帧格式**与 **usage 字段位置**——文档与实际
      返回常有出入。优先级：OpenAI、Anthropic、Gemini、火山引擎、阿里云百炼各一份即可。

## 仅知情，无需回复

- [ ] (08-23)【M0 完成】四条验收标准全部通过，206 个测试全绿，clippy `-D warnings
      -W pedantic` 干净。二进制冒烟通过：启动 → 服务 → SIGTERM 优雅退出。
      偏离清单已写入路线图 M0 小节。
- [ ] (08-23)【开发环境】本机装了 Rust 1.98.0 与 sqlx-cli，fish 配置已加
      `source ~/.cargo/env.fish`。开发库是 Docker 容器 `gw-dev-pg`（端口 5433），
      用 `./scripts/dev-db.sh` 拉起。集成测试需要 `DATABASE_URL` 或
      `TEST_DATABASE_URL`。
- [ ] (08-23)【未落代码】`Coordinator::rate_allow` 与 `try_lock` 只有文档定义，
      使用方（限流、巡检任务）都在 M1，届时随实现一并加入。

- [ ] (08-23)【选型偏离】SSE 解析放弃 `eventsource-stream` 改为自研推模式解析器。
      理由与代价已写入技术基线 §4.4。
